name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: centos-7.6

    steps:
    - uses: actions/checkout@v2

    - name: install
      run: yarn

    - name: generate
      run: yarn generate
